# Ecoscan: Waste Classification Application

This repository contains the source code for the **Ecoscan** application, a cross-platform tool that uses a **MobileNetV2** model deployed via **TensorFlow.js (TF.js)** to classify waste.

---

## 🛠️Setup and Run Tutorial

To run this application locally and verify its functionality, you must have **Node.js** and **npm** installed.

### Execution Steps

Open your terminal and run the following commands in sequence:

```sh
# Step 1: Clone the repository using the project's Git URL.
git clone <YOUR_GIT_URL>

# Step 2: Navigate to the project directory.
cd <YOUR_PROJECT_NAME>

# Step 3: Install necessary dependencies (React, TF.js, Vite, etc.).
npm install

# Step 4: Start the development server.
npm run dev
